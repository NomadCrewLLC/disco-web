import '../../globals.css';

export const metadata = {
  title: 'Disco Login',
  description: '',
};

export default function RootLayout({ children }) {
  return (
    <>
      {children}
    </>
  );
}
